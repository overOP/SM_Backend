export enum Role {
  Student = "student",
  Principal = "principal",
  Teacher = "teacher",
  Parent = "parent",
  Superadmin = "superadmin",
}

export enum Gender {
  Male = "male",
  Female = "female",
  Other = "other",
}

export enum Status {
  Active = "active",
  OnLeave = "onLeave",
  InActive = "inActive",
}

export enum FeeStatus {
  Paid = "paid",
  Due = "due",
}

export enum AttendanceStatus {
  Present = "present",
  Absent = "absent",
}

export enum ResultStatus {
  Pass = "pass",
  NG = "NG",
}
